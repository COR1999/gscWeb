# Sanity Configuration
# Get these values from https://sanity.io/manage

# Required: Your Sanity project ID
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Required: Dataset name (usually 'production' or 'development')
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: API version (defaults to current date if not set)
NEXT_PUBLIC_SANITY_API_VERSION=2025-09-10

# Optional: Only needed if you have a private dataset or need write access
# SANITY_API_TOKEN=your_sanity_token_here